---
import Layout from '../layouts/Layout.astro';

const posts: { title: string; date: string; slug: string; excerpt: string }[] = [];

function formatDate(dateStr: string) {
  return new Date(dateStr).toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });
}
---

<Layout title="Sergei Rastrigin - Building Developer Tools">
  <section class="mb-12">
    <h1 class="text-4xl font-bold mb-4 bg-gradient-to-r from-[var(--color-text)] via-[var(--color-accent)] to-[var(--color-secondary)] bg-clip-text text-transparent">
      Blog
    </h1>
    <p class="text-lg text-[var(--color-text-muted)]">
      Notes on AI, architecture decisions, and lessons from building things.
    </p>
  </section>

  <section>
    <div class="space-y-4">
      {posts.map((post) => (
        <a
          href={`/blog/${post.slug}`}
          class="block p-6 rounded-xl bg-[var(--color-bg-card)] border border-[var(--color-border)] hover:border-[var(--color-accent)] transition-all duration-200 group"
        >
          <div class="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-2 mb-2">
            <h2 class="text-xl font-semibold group-hover:text-[var(--color-accent)] transition-colors">
              {post.title}
            </h2>
            <time class="text-sm text-[var(--color-text-muted)] font-mono shrink-0">
              {formatDate(post.date)}
            </time>
          </div>
          <p class="text-[var(--color-text-muted)]">
            {post.excerpt}
          </p>
        </a>
      ))}
    </div>

    {posts.length === 0 && (
      <p class="text-[var(--color-text-muted)]">
        Nothing here yet.
      </p>
    )}
  </section>
</Layout>
